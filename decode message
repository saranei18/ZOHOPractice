#include<stdio.h>
#include<string.h>
void main()
{
    char a[]={"123"};
    int c1=1,c2=1;
    int c=0;
    for(int i=1;i<strlen(a);i++)
    {
        int d=a[i]-'0';
        int dd=(a[i-1]-'0')*10+d;
        printf("%d %d \n",d,dd);
        c=0;
        if(d>0)c=c+c2;
        if(dd>=10&&dd<=26)c=c+c1;
        c1=c2;
        c2=c;
    }
    printf("%d",c2);
}

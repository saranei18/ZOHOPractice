#include<stdio.h>
#include<math.h>
void main()
{
    int a=20;
    int b=100;
    for(int i=a;i<=100;i++)
    {
        float f=(float)sqrt(i);
        int j=sqrt(i);
        if(f==j)printf("%d ",i);
    }
}
